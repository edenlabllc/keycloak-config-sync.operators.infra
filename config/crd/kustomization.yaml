# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/v1.edp.edenlab.io_keycloaks.yaml
- bases/v1.edp.edenlab.io_keycloakauthflows.yaml
- bases/v1.edp.edenlab.io_keycloakclients.yaml
- bases/v1.edp.edenlab.io_keycloakclientscopes.yaml
- bases/v1.edp.edenlab.io_keycloakrealmcomponents.yaml
- bases/v1.edp.edenlab.io_keycloakrealms.yaml
- bases/v1.edp.edenlab.io_keycloakrealmgroups.yaml
- bases/v1.edp.edenlab.io_keycloakrealmidentityproviders.yaml
- bases/v1.edp.edenlab.io_keycloakrealmroles.yaml
- bases/v1.edp.edenlab.io_keycloakrealmrolebatches.yaml
- bases/v1.edp.edenlab.io_keycloakrealmusers.yaml
- bases/v1.edp.edenlab.io_clusterkeycloaks.yaml
- bases/v1.edp.edenlab.io_clusterkeycloakrealms.yaml
- bases/v1.edp.edenlab.io_keycloakorganizations.yaml
- bases/v1.edp.edenlab.io_keycloakclientscopemappings.yaml
- bases/v1.edp.edenlab.io_keycloakscopemappings.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
