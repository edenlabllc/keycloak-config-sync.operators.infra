---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: keycloakscopemappings.config.idp.edenlab.io
spec:
  group: config.idp.edenlab.io
  names:
    kind: KeycloakScopeMapping
    listKind: KeycloakScopeMappingList
    plural: keycloakscopemappings
    singular: keycloakscopemapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Reconciliation status
      jsonPath: .status.value
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeycloakScopeMapping is the Schema for the keycloakscopemapping
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeycloakScopeMappingSpec defines the desired state of KeycloakClientScopeMapping.
            properties:
              client:
                type: string
              clientScope:
                description: ClientScope of keycloak client scope.
                type: string
              configRef:
                description: ConfigRef is reference to Keycloak Config.
                properties:
                  secret:
                    description: Secret is a secret name which contains admin credentials.
                    type: string
                  url:
                    description: URL of keycloak service.
                    type: string
                required:
                - secret
                - url
                type: object
              description:
                description: Description is a description of client scope.
                type: string
              fromClient:
                type: string
              realmRef:
                description: RealmRef is reference to Realm custom resource.
                properties:
                  kind:
                    default: KeycloakRealm
                    description: Kind specifies the kind of the Keycloak resource.
                    enum:
                    - KeycloakRealm
                    - ClusterKeycloakRealm
                    type: string
                  name:
                    description: Name specifies the name of the Keycloak resource.
                    type: string
                required:
                - name
                type: object
              reconciliationStrategy:
                description: ReconciliationStrategy is a strategy to reconcile client.
                enum:
                - full
                - addOnly
                type: string
              roles:
                description: Roles
                items:
                  properties:
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - configRef
            - realmRef
            - roles
            type: object
          status:
            description: KeycloakScopeMappingStatus defines the observed state of
              KeycloakClientScopeMapping.
            properties:
              failureCount:
                format: int64
                type: integer
              id:
                type: string
              value:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
